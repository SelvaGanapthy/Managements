<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.admin.management.activities.Execution">

    <include
        android:id="@+id/id_executionMain"
        layout="@layout/layout_execution"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="visible">

    </include>

    <include
        android:id="@+id/id_layoutWorkprogress"
        layout="@layout/layoutex_workprogress"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

    </include>

    <include
        android:id="@+id/id_layoutMaterialissue"
        layout="@layout/layoutex_materialissue"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

    </include>

    <include
        android:id="@+id/id_layoutLabourstrength"
        layout="@layout/layoutex_labourstrength"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

    </include>
</RelativeLayout>
